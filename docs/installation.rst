============
Installation
============

At the command line::

    $ easy_install django-indian-currency

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-indian-currency
    $ pip install django-indian-currency
